#include "libft.h"

int main()
{
	double 		d = 10.0143235;
	long double sum = 0;
	int 		count = 0;

	//print_int(ft_five_pow(5), 5);
	ft_printf("%f\n", d);
	count = 0;
	/*while (count < 63)
	{
		sum = ft_double_pow(2, -count);
		printf("%.50Lf %-25lu %5d\n\n", sum, ft_five_pow(count), count);
		count++;
	}*/
	return 0;
}